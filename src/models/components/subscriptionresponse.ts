/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Subscription is active and scheduled to renew
 */
export const SubscriptionResponseStatusType = {
  Zero: 0,
  One: 1,
  Two: 2,
} as const;
/**
 * Subscription is active and scheduled to renew
 */
export type SubscriptionResponseStatusType = ClosedEnum<
  typeof SubscriptionResponseStatusType
>;

export type SubscriptionResponse = {
  id: string;
  userId: string;
  /**
   * List of SKUs subscribed to
   */
  skuIds: Array<string>;
  /**
   * List of SKUs that this user will be subscribed to at renewal
   */
  renewalSkuIds: Array<string> | null;
  /**
   * List of entitlements granted for this subscription
   */
  entitlementIds: Array<string>;
  /**
   * Start of the current subscription period
   */
  currentPeriodStart: Date;
  /**
   * End of the current subscription period
   */
  currentPeriodEnd: Date;
  status: SubscriptionResponseStatusType;
  /**
   * When the subscription was canceled
   */
  canceledAt: Date | null;
  /**
   * ISO3166-1 alpha-2 country code of the payment source used to purchase the subscription
   */
  country?: string | null | undefined;
};

/** @internal */
export const SubscriptionResponseStatusType$inboundSchema: z.ZodNativeEnum<
  typeof SubscriptionResponseStatusType
> = z.nativeEnum(SubscriptionResponseStatusType);
/** @internal */
export const SubscriptionResponseStatusType$outboundSchema: z.ZodNativeEnum<
  typeof SubscriptionResponseStatusType
> = SubscriptionResponseStatusType$inboundSchema;

/** @internal */
export const SubscriptionResponse$inboundSchema: z.ZodType<
  SubscriptionResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  user_id: z.string(),
  sku_ids: z.array(z.string()),
  renewal_sku_ids: z.nullable(z.array(z.string())),
  entitlement_ids: z.array(z.string()),
  current_period_start: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  current_period_end: z.string().datetime({ offset: true }).transform(v =>
    new Date(v)
  ),
  status: SubscriptionResponseStatusType$inboundSchema,
  canceled_at: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ),
  country: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "user_id": "userId",
    "sku_ids": "skuIds",
    "renewal_sku_ids": "renewalSkuIds",
    "entitlement_ids": "entitlementIds",
    "current_period_start": "currentPeriodStart",
    "current_period_end": "currentPeriodEnd",
    "canceled_at": "canceledAt",
  });
});
/** @internal */
export type SubscriptionResponse$Outbound = {
  id: string;
  user_id: string;
  sku_ids: Array<string>;
  renewal_sku_ids: Array<string> | null;
  entitlement_ids: Array<string>;
  current_period_start: string;
  current_period_end: string;
  status: number;
  canceled_at: string | null;
  country?: string | null | undefined;
};

/** @internal */
export const SubscriptionResponse$outboundSchema: z.ZodType<
  SubscriptionResponse$Outbound,
  z.ZodTypeDef,
  SubscriptionResponse
> = z.object({
  id: z.string(),
  userId: z.string(),
  skuIds: z.array(z.string()),
  renewalSkuIds: z.nullable(z.array(z.string())),
  entitlementIds: z.array(z.string()),
  currentPeriodStart: z.date().transform(v => v.toISOString()),
  currentPeriodEnd: z.date().transform(v => v.toISOString()),
  status: SubscriptionResponseStatusType$outboundSchema,
  canceledAt: z.nullable(z.date().transform(v => v.toISOString())),
  country: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    userId: "user_id",
    skuIds: "sku_ids",
    renewalSkuIds: "renewal_sku_ids",
    entitlementIds: "entitlement_ids",
    currentPeriodStart: "current_period_start",
    currentPeriodEnd: "current_period_end",
    canceledAt: "canceled_at",
  });
});

export function subscriptionResponseToJSON(
  subscriptionResponse: SubscriptionResponse,
): string {
  return JSON.stringify(
    SubscriptionResponse$outboundSchema.parse(subscriptionResponse),
  );
}
export function subscriptionResponseFromJSON(
  jsonString: string,
): SafeParseResult<SubscriptionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubscriptionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubscriptionResponse' from JSON`,
  );
}
