/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ScheduledEventUserCountResponse = {
  /**
   * The number of users subscribed to the scheduled event
   */
  guildScheduledEventCount: number;
  /**
   * Map of exception IDs to user counts for each exception
   */
  guildScheduledEventExceptionCounts: { [k: string]: number };
};

/** @internal */
export const ScheduledEventUserCountResponse$inboundSchema: z.ZodType<
  ScheduledEventUserCountResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  guild_scheduled_event_count: z.number().int(),
  guild_scheduled_event_exception_counts: z.record(z.number().int()),
}).transform((v) => {
  return remap$(v, {
    "guild_scheduled_event_count": "guildScheduledEventCount",
    "guild_scheduled_event_exception_counts":
      "guildScheduledEventExceptionCounts",
  });
});
/** @internal */
export type ScheduledEventUserCountResponse$Outbound = {
  guild_scheduled_event_count: number;
  guild_scheduled_event_exception_counts: { [k: string]: number };
};

/** @internal */
export const ScheduledEventUserCountResponse$outboundSchema: z.ZodType<
  ScheduledEventUserCountResponse$Outbound,
  z.ZodTypeDef,
  ScheduledEventUserCountResponse
> = z.object({
  guildScheduledEventCount: z.number().int(),
  guildScheduledEventExceptionCounts: z.record(z.number().int()),
}).transform((v) => {
  return remap$(v, {
    guildScheduledEventCount: "guild_scheduled_event_count",
    guildScheduledEventExceptionCounts:
      "guild_scheduled_event_exception_counts",
  });
});

export function scheduledEventUserCountResponseToJSON(
  scheduledEventUserCountResponse: ScheduledEventUserCountResponse,
): string {
  return JSON.stringify(
    ScheduledEventUserCountResponse$outboundSchema.parse(
      scheduledEventUserCountResponse,
    ),
  );
}
export function scheduledEventUserCountResponseFromJSON(
  jsonString: string,
): SafeParseResult<ScheduledEventUserCountResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ScheduledEventUserCountResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ScheduledEventUserCountResponse' from JSON`,
  );
}
