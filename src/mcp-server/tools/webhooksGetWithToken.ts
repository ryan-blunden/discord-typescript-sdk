/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetWithToken } from "../../funcs/webhooksGetWithToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWebhookByTokenRequest$inboundSchema,
};

export const tool$webhooksGetWithToken: ToolDefinition<typeof args> = {
  name: "webhooks-get-with-token",
  description:
    `Same as above, except this call does not require authentication and returns no user in the webhook object.`,
  scopes: ["webhooks", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksGetWithToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
