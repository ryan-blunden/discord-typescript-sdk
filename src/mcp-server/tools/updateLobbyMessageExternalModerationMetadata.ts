/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { updateLobbyMessageExternalModerationMetadata } from "../../funcs/updateLobbyMessageExternalModerationMetadata.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .UpdateLobbyMessageExternalModerationMetadataRequest$inboundSchema,
};

export const tool$updateLobbyMessageExternalModerationMetadata: ToolDefinition<
  typeof args
> = {
  name: "update-lobby-message-external-moderation-metadata",
  description: `Update the external moderation metadata for a lobby message.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await updateLobbyMessageExternalModerationMetadata(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
