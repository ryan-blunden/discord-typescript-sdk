/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceUpdateUserState } from "../../funcs/voiceUpdateUserState.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateVoiceStateRequest$inboundSchema,
};

export const tool$voiceUpdateUserState: ToolDefinition<typeof args> = {
  name: "voice-update-user-state",
  description:
    `Updates another user's voice state. Fires a Voice State Update Gateway event.`,
  scopes: ["voice", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceUpdateUserState(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
