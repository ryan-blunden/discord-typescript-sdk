/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersGetGuildMember } from "../../funcs/usersGetGuildMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMyGuildMemberRequest$inboundSchema,
};

export const tool$usersGetGuildMember: ToolDefinition<typeof args> = {
  name: "users-get-guild-member",
  description:
    `Returns a guild member object for the current user. Requires the guilds.members.read OAuth2 scope.`,
  scopes: ["users", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersGetGuildMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
