/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soundboardsGetGuildSoundboardSound } from "../../funcs/soundboardsGetGuildSoundboardSound.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildSoundboardSoundRequest$inboundSchema,
};

export const tool$soundboardsGetGuildSoundboardSound: ToolDefinition<
  typeof args
> = {
  name: "soundboards-get-guild-soundboard-sound",
  description:
    `Returns a soundboard sound object for the given guild and sound IDs.`,
  scopes: ["soundboard", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await soundboardsGetGuildSoundboardSound(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
