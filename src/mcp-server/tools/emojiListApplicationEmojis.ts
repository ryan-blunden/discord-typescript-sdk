/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emojiListApplicationEmojis } from "../../funcs/emojiListApplicationEmojis.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListApplicationEmojisRequest$inboundSchema,
};

export const tool$emojiListApplicationEmojis: ToolDefinition<typeof args> = {
  name: "emoji-list-application-emojis",
  description:
    `Returns an object containing a list of emoji objects for the given application under the items key. Includes a user object for the team member that uploaded the emoji from the app's settings, or for the bot user if uploaded using the API.`,
  scopes: ["emoji", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emojiListApplicationEmojis(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
