/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { countGuildScheduledEventUsers } from "../../funcs/countGuildScheduledEventUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CountGuildScheduledEventUsersRequest$inboundSchema,
};

export const tool$countGuildScheduledEventUsers: ToolDefinition<typeof args> = {
  name: "count-guild-scheduled-event-users",
  description: `Get the count of users subscribed to a guild scheduled event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await countGuildScheduledEventUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
