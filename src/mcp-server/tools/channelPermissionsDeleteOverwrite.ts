/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelPermissionsDeleteOverwrite } from "../../funcs/channelPermissionsDeleteOverwrite.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteChannelPermissionOverwriteRequest$inboundSchema,
};

export const tool$channelPermissionsDeleteOverwrite: ToolDefinition<
  typeof args
> = {
  name: "channel-permissions_delete-overwrite",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelPermissionsDeleteOverwrite(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
