/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lobbiesGet } from "../../funcs/lobbiesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLobbyRequest$inboundSchema,
};

export const tool$lobbiesGet: ToolDefinition<typeof args> = {
  name: "lobbies-get",
  description: `Returns the lobby object for the given id.`,
  scopes: ["lobby", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lobbiesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
