/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deleteLobbyMember } from "../../funcs/deleteLobbyMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLobbyMemberRequest$inboundSchema,
};

export const tool$deleteLobbyMember: ToolDefinition<typeof args> = {
  name: "delete-lobby-member",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deleteLobbyMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
