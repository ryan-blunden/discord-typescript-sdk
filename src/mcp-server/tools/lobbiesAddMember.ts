/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lobbiesAddMember } from "../../funcs/lobbiesAddMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddLobbyMemberRequest$inboundSchema,
};

export const tool$lobbiesAddMember: ToolDefinition<typeof args> = {
  name: "lobbies-add-member",
  description: `Adds a user to a lobby.`,
  scopes: ["lobby", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lobbiesAddMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
