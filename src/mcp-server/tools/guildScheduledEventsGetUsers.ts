/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildScheduledEventsGetUsers } from "../../funcs/guildScheduledEventsGetUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildScheduledEventUsersRequest$inboundSchema,
};

export const tool$guildScheduledEventsGetUsers: ToolDefinition<typeof args> = {
  name: "guild-scheduled-events-get-users",
  description:
    `Get a list of guild scheduled event users subscribed to a guild scheduled event. Returns a list of guild scheduled event user objects on success. Guild member data, if it exists, is included if the with_member query parameter is set.`,
  scopes: ["guild-scheduled-event", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildScheduledEventsGetUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
