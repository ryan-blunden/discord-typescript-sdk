/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { botPartnerSDKToken } from "../../funcs/botPartnerSDKToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.BotPartnerSDKTokenRequestBody$inboundSchema,
};

export const tool$botPartnerSDKToken: ToolDefinition<typeof args> = {
  name: "bot-partner-SDK-token",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await botPartnerSDKToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
