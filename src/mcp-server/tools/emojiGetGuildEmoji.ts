/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emojiGetGuildEmoji } from "../../funcs/emojiGetGuildEmoji.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildEmojiRequest$inboundSchema,
};

export const tool$emojiGetGuildEmoji: ToolDefinition<typeof args> = {
  name: "emoji-get-guild-emoji",
  description:
    `Returns an emoji object for the given guild and emoji IDs. Includes the user field if the bot has the MANAGE_GUILD_EXPRESSIONS permission, or if the bot created the emoji and has the the CREATE_GUILD_EXPRESSIONS permission.`,
  scopes: ["emoji", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emojiGetGuildEmoji(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
