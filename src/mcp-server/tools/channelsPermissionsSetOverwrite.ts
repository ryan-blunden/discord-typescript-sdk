/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsPermissionsSetOverwrite } from "../../funcs/channelsPermissionsSetOverwrite.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetChannelPermissionOverwriteRequest$inboundSchema,
};

export const tool$channelsPermissionsSetOverwrite: ToolDefinition<typeof args> =
  {
    name: "channels-permissions-set-overwrite",
    description: ``,
    scopes: ["write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await channelsPermissionsSetOverwrite(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
