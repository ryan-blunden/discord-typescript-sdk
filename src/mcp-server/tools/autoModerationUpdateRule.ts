/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { autoModerationUpdateRule } from "../../funcs/autoModerationUpdateRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAutoModerationRuleRequest$inboundSchema,
};

export const tool$autoModerationUpdateRule: ToolDefinition<typeof args> = {
  name: "auto-moderation-update-rule",
  description:
    `Modify an existing rule. Returns an auto moderation rule on success. Fires an Auto Moderation Rule Update Gateway event.`,
  scopes: ["auto-moderation", "write", "auto-moderation", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await autoModerationUpdateRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
