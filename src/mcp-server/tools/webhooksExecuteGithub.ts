/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksExecuteGithub } from "../../funcs/webhooksExecuteGithub.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ExecuteGithubCompatibleWebhookRequest$inboundSchema,
};

export const tool$webhooksExecuteGithub: ToolDefinition<typeof args> = {
  name: "webhooks-execute-github",
  description:
    `Add a new webhook to your GitHub repo (in the repo's settings), and use this endpoint as the "Payload URL."`,
  scopes: ["webhooks", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksExecuteGithub(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
