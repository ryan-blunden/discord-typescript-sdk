/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksListForGuild } from "../../funcs/webhooksListForGuild.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildWebhooksRequest$inboundSchema,
};

export const tool$webhooksListForGuild: ToolDefinition<typeof args> = {
  name: "webhooks-list-for-guild",
  description:
    `Returns a list of guild webhook objects. Requires the MANAGE_WEBHOOKS permission.`,
  scopes: ["webhooks", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksListForGuild(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
