/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceUpdateCurrentUserState } from "../../funcs/voiceUpdateCurrentUserState.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSelfVoiceStateRequest$inboundSchema,
};

export const tool$voiceUpdateCurrentUserState: ToolDefinition<typeof args> = {
  name: "voice-update-current-user-state",
  description:
    `Updates the current user's voice state. Returns 204 No Content on success. Fires a Voice State Update Gateway event.`,
  scopes: ["voice", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceUpdateCurrentUserState(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
