/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emojiGetApplicationEmoji } from "../../funcs/emojiGetApplicationEmoji.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApplicationEmojiRequest$inboundSchema,
};

export const tool$emojiGetApplicationEmoji: ToolDefinition<typeof args> = {
  name: "emoji-get-application-emoji",
  description:
    `Returns an emoji object for the given application and emoji IDs. Includes the user field.`,
  scopes: ["emoji", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emojiGetApplicationEmoji(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
