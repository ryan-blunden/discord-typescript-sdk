/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsTriggerTypingIndicator } from "../../funcs/channelsTriggerTypingIndicator.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TriggerTypingIndicatorRequest$inboundSchema,
};

export const tool$channelsTriggerTypingIndicator: ToolDefinition<typeof args> =
  {
    name: "channels-trigger-typing-indicator",
    description:
      `Post a typing indicator for the specified channel, which expires after 10 seconds. Returns a 204 empty response on success. Fires a Typing Start Gateway event.`,
    scopes: ["channels", "write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await channelsTriggerTypingIndicator(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
