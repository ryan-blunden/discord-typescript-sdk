/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersLeaveGuild } from "../../funcs/usersLeaveGuild.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LeaveGuildRequest$inboundSchema,
};

export const tool$usersLeaveGuild: ToolDefinition<typeof args> = {
  name: "users-leave-guild",
  description:
    `Leave a guild. Returns a 204 empty response on success. Fires a Guild Delete Gateway event and a Guild Member Remove Gateway event.`,
  scopes: ["users", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersLeaveGuild(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
