/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { autoModerationListRules } from "../../funcs/autoModerationListRules.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListAutoModerationRulesRequest$inboundSchema,
};

export const tool$autoModerationListRules: ToolDefinition<typeof args> = {
  name: "auto-moderation-list-rules",
  description:
    `Get a list of all rules currently configured for the guild. Returns a list of auto moderation rule objects for the given guild. This endpoint requires the MANAGE_GUILD permission.`,
  scopes: ["auto-moderation", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await autoModerationListRules(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
