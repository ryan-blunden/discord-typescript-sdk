/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitesDelete } from "../../funcs/invitesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InviteRevokeRequest$inboundSchema,
};

export const tool$invitesDelete: ToolDefinition<typeof args> = {
  name: "invites-delete",
  description:
    `Delete an invite. Requires the MANAGE_CHANNELS permission on the channel this invite belongs to, or MANAGE_GUILD to remove any invite across the guild. Returns an invite object on success.`,
  scopes: ["invites", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
