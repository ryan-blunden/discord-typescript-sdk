/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emojiUpdateApplicationEmoji } from "../../funcs/emojiUpdateApplicationEmoji.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateApplicationEmojiRequest$inboundSchema,
};

export const tool$emojiUpdateApplicationEmoji: ToolDefinition<typeof args> = {
  name: "emoji-update-application-emoji",
  description:
    `Modify the given emoji. Returns the updated emoji object on success.`,
  scopes: ["emoji", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emojiUpdateApplicationEmoji(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
