/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lobbiesRemoveMember } from "../../funcs/lobbiesRemoveMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLobbyMemberRequest$inboundSchema,
};

export const tool$lobbiesRemoveMember: ToolDefinition<typeof args> = {
  name: "lobbies-remove-member",
  description: `Removes a user from a lobby.`,
  scopes: ["lobby", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lobbiesRemoveMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
