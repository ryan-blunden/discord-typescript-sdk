/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsSearchMembers } from "../../funcs/guildsSearchMembers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SearchGuildMembersRequest$inboundSchema,
};

export const tool$guildsSearchMembers: ToolDefinition<typeof args> = {
  name: "guilds-search-members",
  description:
    `Returns a list of guild member objects whose username or nickname starts with a provided string.`,
  scopes: ["guilds", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsSearchMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
