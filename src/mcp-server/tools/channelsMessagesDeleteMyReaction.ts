/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsMessagesDeleteMyReaction } from "../../funcs/channelsMessagesDeleteMyReaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteMyMessageReactionRequest$inboundSchema,
};

export const tool$channelsMessagesDeleteMyReaction: ToolDefinition<
  typeof args
> = {
  name: "channels-messages_delete-my-reaction",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsMessagesDeleteMyReaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
