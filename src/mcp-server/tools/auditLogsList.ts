/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { auditLogsList } from "../../funcs/auditLogsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildAuditLogEntriesRequest$inboundSchema,
};

export const tool$auditLogsList: ToolDefinition<typeof args> = {
  name: "audit-logs-list",
  description:
    `Returns an audit log object for the guild. Requires the VIEW_AUDIT_LOG permission.`,
  scopes: ["audit-logs", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await auditLogsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
