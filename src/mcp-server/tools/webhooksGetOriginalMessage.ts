/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetOriginalMessage } from "../../funcs/webhooksGetOriginalMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOriginalWebhookMessageRequest$inboundSchema,
};

export const tool$webhooksGetOriginalMessage: ToolDefinition<typeof args> = {
  name: "webhooks-get-original-message",
  description: `Returns the initial webhook message.`,
  scopes: ["webhooks", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksGetOriginalMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
