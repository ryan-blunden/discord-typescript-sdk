/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsGetPublicKeys } from "../../funcs/applicationsGetPublicKeys.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$applicationsGetPublicKeys: ToolDefinition = {
  name: "applications-get-public-keys",
  description: `Returns a list of public keys used for verifying signatures.`,
  scopes: ["application", "read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await applicationsGetPublicKeys(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
