/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsSetMfaLevel } from "../../funcs/guildsSetMfaLevel.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SetGuildMfaLevelRequest$inboundSchema,
};

export const tool$guildsSetMfaLevel: ToolDefinition<typeof args> = {
  name: "guilds-set-mfa-level",
  description:
    `Modify a guild's MFA level. Requires guild ownership. Returns the updated level on success. Fires a Guild Update Gateway event.`,
  scopes: ["guilds", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsSetMfaLevel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
