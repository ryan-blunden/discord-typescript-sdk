/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsListPublicArchivedThreads } from "../../funcs/channelsListPublicArchivedThreads.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPublicArchivedThreadsRequest$inboundSchema,
};

export const tool$channelsListPublicArchivedThreads: ToolDefinition<
  typeof args
> = {
  name: "channels-list-public-archived-threads",
  description:
    `Returns archived threads in the channel that are public. When called on a GUILD_TEXT channel, returns threads of type PUBLIC_THREAD. When called on a GUILD_ANNOUNCEMENT channel returns threads of type ANNOUNCEMENT_THREAD. Threads are ordered by archive_timestamp, in descending order. Requires the READ_MESSAGE_HISTORY permission.`,
  scopes: ["channels", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsListPublicArchivedThreads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
