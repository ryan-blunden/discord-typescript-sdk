/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bulkUpdateLobbyMembers } from "../../funcs/bulkUpdateLobbyMembers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.BulkUpdateLobbyMembersRequest$inboundSchema,
};

export const tool$bulkUpdateLobbyMembers: ToolDefinition<typeof args> = {
  name: "bulk-update-lobby-members",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bulkUpdateLobbyMembers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
