/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { createLinkedLobbyGuildInviteForUser } from "../../funcs/createLinkedLobbyGuildInviteForUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLinkedLobbyGuildInviteForUserRequest$inboundSchema,
};

export const tool$createLinkedLobbyGuildInviteForUser: ToolDefinition<
  typeof args
> = {
  name: "create-linked-lobby-guild-invite-for-user",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await createLinkedLobbyGuildInviteForUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
