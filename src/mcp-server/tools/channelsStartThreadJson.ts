/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsStartThreadJson } from "../../funcs/channelsStartThreadJson.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateThreadJsonRequest$inboundSchema,
};

export const tool$channelsStartThreadJson: ToolDefinition<typeof args> = {
  name: "channels-start-thread-json",
  description:
    `Creates a new thread that is not connected to an existing message. Returns a channel on success, and a 400 BAD REQUEST on invalid parameters. Fires a Thread Create Gateway event.`,
  scopes: ["channels", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsStartThreadJson(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
