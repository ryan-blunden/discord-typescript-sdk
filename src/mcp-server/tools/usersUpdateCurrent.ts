/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersUpdateCurrent } from "../../funcs/usersUpdateCurrent.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.BotAccountPatchRequest$inboundSchema,
};

export const tool$usersUpdateCurrent: ToolDefinition<typeof args> = {
  name: "users-update-current",
  description:
    `Modify the requester's user account settings. Returns a user object on success. Fires a User Update Gateway event.`,
  scopes: ["users", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await usersUpdateCurrent(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
