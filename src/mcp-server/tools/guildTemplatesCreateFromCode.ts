/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesCreateFromCode } from "../../funcs/guildTemplatesCreateFromCode.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGuildFromTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesCreateFromCode: ToolDefinition<typeof args> = {
  name: "guild-templates_create-from-code",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesCreateFromCode(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
