/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesSync } from "../../funcs/guildTemplatesSync.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SyncGuildTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesSync: ToolDefinition<typeof args> = {
  name: "guild-templates-sync",
  description:
    `Syncs the template to the guild's current state. Requires the MANAGE_GUILD permission. Returns the guild template object on success.`,
  scopes: ["guild-template", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesSync(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
