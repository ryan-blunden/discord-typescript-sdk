/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildOnboardingGet } from "../../funcs/guildOnboardingGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildsOnboardingRequest$inboundSchema,
};

export const tool$guildOnboardingGet: ToolDefinition<typeof args> = {
  name: "guild-onboarding-get",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildOnboardingGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
