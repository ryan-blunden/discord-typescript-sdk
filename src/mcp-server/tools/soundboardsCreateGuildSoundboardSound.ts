/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soundboardsCreateGuildSoundboardSound } from "../../funcs/soundboardsCreateGuildSoundboardSound.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGuildSoundboardSoundRequest$inboundSchema,
};

export const tool$soundboardsCreateGuildSoundboardSound: ToolDefinition<
  typeof args
> = {
  name: "soundboards-create-guild-soundboard-sound",
  description:
    `Create a new soundboard sound for the guild. Returns the new soundboard sound object on success.`,
  scopes: ["soundboard", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await soundboardsCreateGuildSoundboardSound(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
