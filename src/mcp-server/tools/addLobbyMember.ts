/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addLobbyMember } from "../../funcs/addLobbyMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddLobbyMemberRequest$inboundSchema,
};

export const tool$addLobbyMember: ToolDefinition<typeof args> = {
  name: "add-lobby-member",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await addLobbyMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
