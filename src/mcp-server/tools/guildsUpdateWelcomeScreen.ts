/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsUpdateWelcomeScreen } from "../../funcs/guildsUpdateWelcomeScreen.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGuildWelcomeScreenRequest$inboundSchema,
};

export const tool$guildsUpdateWelcomeScreen: ToolDefinition<typeof args> = {
  name: "guilds_update-welcome-screen",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsUpdateWelcomeScreen(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
