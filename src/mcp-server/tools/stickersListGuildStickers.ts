/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stickersListGuildStickers } from "../../funcs/stickersListGuildStickers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildStickersRequest$inboundSchema,
};

export const tool$stickersListGuildStickers: ToolDefinition<typeof args> = {
  name: "stickers-list-guild-stickers",
  description:
    `Returns an array of sticker objects for the given guild. Includes user fields if the bot has the CREATE_GUILD_EXPRESSIONS or MANAGE_GUILD_EXPRESSIONS permission.`,
  scopes: ["stickers", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stickersListGuildStickers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
