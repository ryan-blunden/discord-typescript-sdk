/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soundboardsUpdateGuildSoundboardSound } from "../../funcs/soundboardsUpdateGuildSoundboardSound.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGuildSoundboardSoundRequest$inboundSchema,
};

export const tool$soundboardsUpdateGuildSoundboardSound: ToolDefinition<
  typeof args
> = {
  name: "soundboards-update-guild-soundboard-sound",
  description:
    `Update a soundboard sound in a guild. Returns the updated soundboard sound object on success.`,
  scopes: ["soundboard", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await soundboardsUpdateGuildSoundboardSound(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
