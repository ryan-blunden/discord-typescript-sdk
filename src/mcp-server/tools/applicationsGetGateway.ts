/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsGetGateway } from "../../funcs/applicationsGetGateway.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$applicationsGetGateway: ToolDefinition = {
  name: "applications-get-gateway",
  description:
    `Returns an object with a single valid WSS URL, which the client can use for connecting to Discord.`,
  scopes: ["application", "read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await applicationsGetGateway(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
