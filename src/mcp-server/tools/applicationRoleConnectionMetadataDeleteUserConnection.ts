/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationRoleConnectionMetadataDeleteUserConnection } from "../../funcs/applicationRoleConnectionMetadataDeleteUserConnection.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApplicationUserRoleConnectionRequest$inboundSchema,
};

export const tool$applicationRoleConnectionMetadataDeleteUserConnection:
  ToolDefinition<typeof args> = {
    name: "application-role-connection-metadata-delete-user-connection",
    description: `Deletes the application role connection for the user.`,
    scopes: ["application-role-connection-metadata", "write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await applicationRoleConnectionMetadataDeleteUserConnection(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
