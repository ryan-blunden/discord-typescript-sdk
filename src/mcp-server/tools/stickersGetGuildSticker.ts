/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stickersGetGuildSticker } from "../../funcs/stickersGetGuildSticker.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildStickerRequest$inboundSchema,
};

export const tool$stickersGetGuildSticker: ToolDefinition<typeof args> = {
  name: "stickers-get-guild-sticker",
  description:
    `Returns a sticker object for the given guild and sticker IDs. Includes the user field if the bot has the CREATE_GUILD_EXPRESSIONS or MANAGE_GUILD_EXPRESSIONS permission.`,
  scopes: ["stickers", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stickersGetGuildSticker(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
