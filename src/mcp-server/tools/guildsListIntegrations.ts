/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsListIntegrations } from "../../funcs/guildsListIntegrations.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildIntegrationsRequest$inboundSchema,
};

export const tool$guildsListIntegrations: ToolDefinition<typeof args> = {
  name: "guilds-list-integrations",
  description:
    `Returns a list of integration objects for the guild. Requires the MANAGE_GUILD permission.`,
  scopes: ["guilds", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsListIntegrations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
