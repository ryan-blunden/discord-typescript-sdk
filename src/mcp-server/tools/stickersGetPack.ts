/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stickersGetPack } from "../../funcs/stickersGetPack.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStickerPackRequest$inboundSchema,
};

export const tool$stickersGetPack: ToolDefinition<typeof args> = {
  name: "stickers-get-pack",
  description: `Returns a sticker pack object for the given sticker pack ID.`,
  scopes: ["stickers", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stickersGetPack(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
