/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesList } from "../../funcs/messagesList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMessagesRequest$inboundSchema,
};

export const tool$messagesList: ToolDefinition<typeof args> = {
  name: "messages-list",
  description:
    `Returns the messages for a channel. If operating on a guild channel, this endpoint requires the VIEW_CHANNEL permission to be present on the current user.`,
  scopes: ["messages", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
