/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksDeleteMessage } from "../../funcs/webhooksDeleteMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteWebhookMessageRequest$inboundSchema,
};

export const tool$webhooksDeleteMessage: ToolDefinition<typeof args> = {
  name: "webhooks-delete-message",
  description:
    `Deletes a message that was created by the webhook. Returns a 204 No Content response on success.`,
  scopes: ["webhooks", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksDeleteMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
