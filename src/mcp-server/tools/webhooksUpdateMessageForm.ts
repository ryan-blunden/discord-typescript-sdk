/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksUpdateMessageForm } from "../../funcs/webhooksUpdateMessageForm.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateWebhookMessageFormRequest$inboundSchema,
};

export const tool$webhooksUpdateMessageForm: ToolDefinition<typeof args> = {
  name: "webhooks-update-message-form",
  description:
    `Edits a previously-sent webhook message from the same token. Returns a message object on success.`,
  scopes: ["webhooks", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksUpdateMessageForm(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
