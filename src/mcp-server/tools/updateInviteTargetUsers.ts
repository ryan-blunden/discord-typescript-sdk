/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { updateInviteTargetUsers } from "../../funcs/updateInviteTargetUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateInviteTargetUsersRequest$inboundSchema,
};

export const tool$updateInviteTargetUsers: ToolDefinition<typeof args> = {
  name: "update-invite-target-users",
  description: `Update the target users for an existing invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await updateInviteTargetUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
