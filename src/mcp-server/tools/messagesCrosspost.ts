/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesCrosspost } from "../../funcs/messagesCrosspost.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CrosspostMessageRequest$inboundSchema,
};

export const tool$messagesCrosspost: ToolDefinition<typeof args> = {
  name: "messages-crosspost",
  description:
    `Crosspost a message in an Announcement Channel to following channels. This endpoint requires the SEND_MESSAGES permission, if the current user sent the message, or additionally the MANAGE_MESSAGES permission, for all other messages, to be present for the current user.`,
  scopes: ["messages", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesCrosspost(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
