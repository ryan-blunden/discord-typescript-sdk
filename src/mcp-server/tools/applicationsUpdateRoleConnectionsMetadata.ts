/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsUpdateRoleConnectionsMetadata } from "../../funcs/applicationsUpdateRoleConnectionsMetadata.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.UpdateApplicationRoleConnectionsMetadataRequest$inboundSchema,
};

export const tool$applicationsUpdateRoleConnectionsMetadata: ToolDefinition<
  typeof args
> = {
  name: "applications_update-role-connections-metadata",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationsUpdateRoleConnectionsMetadata(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
