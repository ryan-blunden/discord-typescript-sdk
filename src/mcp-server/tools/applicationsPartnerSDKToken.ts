/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsPartnerSDKToken } from "../../funcs/applicationsPartnerSDKToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PartnerSDKTokenRequestBody$inboundSchema,
};

export const tool$applicationsPartnerSDKToken: ToolDefinition<typeof args> = {
  name: "applications-partner-SDK-token",
  description: `Gets a token for the partner SDK.`,
  scopes: ["application", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationsPartnerSDKToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
