/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getGuildJoinRequests } from "../../funcs/getGuildJoinRequests.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildJoinRequestsRequest$inboundSchema,
};

export const tool$getGuildJoinRequests: ToolDefinition<typeof args> = {
  name: "get-guild-join-requests",
  description:
    `List join requests for guild, optionally filtered by application status`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getGuildJoinRequests(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
