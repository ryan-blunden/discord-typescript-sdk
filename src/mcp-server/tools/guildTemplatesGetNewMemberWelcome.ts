/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesGetNewMemberWelcome } from "../../funcs/guildTemplatesGetNewMemberWelcome.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildNewMemberWelcomeRequest$inboundSchema,
};

export const tool$guildTemplatesGetNewMemberWelcome: ToolDefinition<
  typeof args
> = {
  name: "guild-templates-get-new-member-welcome",
  description: `Returns the welcome screen object for the guild.`,
  scopes: ["guild-template", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesGetNewMemberWelcome(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
