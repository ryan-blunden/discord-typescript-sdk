/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getInviteTargetUsers } from "../../funcs/getInviteTargetUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInviteTargetUsersRequest$inboundSchema,
};

export const tool$getInviteTargetUsers: ToolDefinition<typeof args> = {
  name: "get-invite-target-users",
  description: `Get the target users for an invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getInviteTargetUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
