/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bulkRemoveInviteTargetUsers } from "../../funcs/bulkRemoveInviteTargetUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.BulkRemoveInviteTargetUsersRequest$inboundSchema,
};

export const tool$bulkRemoveInviteTargetUsers: ToolDefinition<typeof args> = {
  name: "bulk-remove-invite-target-users",
  description: `Remove multiple target users from an existing invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bulkRemoveInviteTargetUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
