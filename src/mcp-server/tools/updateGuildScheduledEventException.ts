/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { updateGuildScheduledEventException } from "../../funcs/updateGuildScheduledEventException.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGuildScheduledEventExceptionRequest$inboundSchema,
};

export const tool$updateGuildScheduledEventException: ToolDefinition<
  typeof args
> = {
  name: "update-guild-scheduled-event-exception",
  description: `Modify an exception to a recurring guild scheduled event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await updateGuildScheduledEventException(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
