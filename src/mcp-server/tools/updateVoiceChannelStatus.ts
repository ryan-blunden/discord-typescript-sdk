/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { updateVoiceChannelStatus } from "../../funcs/updateVoiceChannelStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateVoiceChannelStatusRequest$inboundSchema,
};

export const tool$updateVoiceChannelStatus: ToolDefinition<typeof args> = {
  name: "update-voice-channel-status",
  description: `Set a voice channel's status.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await updateVoiceChannelStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
