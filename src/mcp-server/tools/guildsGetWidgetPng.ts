/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsGetWidgetPng } from "../../funcs/guildsGetWidgetPng.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildWidgetPngRequest$inboundSchema,
};

export const tool$guildsGetWidgetPng: ToolDefinition<typeof args> = {
  name: "guilds-get-widget-png",
  description:
    `Returns a PNG image widget for the guild. Requires no permissions or authentication.`,
  scopes: ["guilds", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsGetWidgetPng(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
