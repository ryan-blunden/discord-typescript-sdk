/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soundboardsSendSoundboardSound } from "../../funcs/soundboardsSendSoundboardSound.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SendSoundboardSoundRequest$inboundSchema,
};

export const tool$soundboardsSendSoundboardSound: ToolDefinition<typeof args> =
  {
    name: "soundboards-send-soundboard-sound",
    description: `Sends a soundboard sound to a channel.`,
    scopes: ["soundboard", "write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await soundboardsSendSoundboardSound(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
