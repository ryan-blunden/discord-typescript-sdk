/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { removeInviteTargetUser } from "../../funcs/removeInviteTargetUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RemoveInviteTargetUserRequest$inboundSchema,
};

export const tool$removeInviteTargetUser: ToolDefinition<typeof args> = {
  name: "remove-invite-target-user",
  description: `Remove a target user from an existing invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await removeInviteTargetUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
