/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksDeleteOriginalMessage } from "../../funcs/webhooksDeleteOriginalMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOriginalWebhookMessageRequest$inboundSchema,
};

export const tool$webhooksDeleteOriginalMessage: ToolDefinition<typeof args> = {
  name: "webhooks-delete-original-message",
  description: `Deletes the initial webhook message.`,
  scopes: ["webhooks", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksDeleteOriginalMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
