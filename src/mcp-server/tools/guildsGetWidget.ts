/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsGetWidget } from "../../funcs/guildsGetWidget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildWidgetRequest$inboundSchema,
};

export const tool$guildsGetWidget: ToolDefinition<typeof args> = {
  name: "guilds-get-widget",
  description:
    `Returns the widget for the guild. Fires an Invite Create Gateway event when an invite channel is defined and a new Invite is generated.`,
  scopes: ["guilds", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsGetWidget(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
