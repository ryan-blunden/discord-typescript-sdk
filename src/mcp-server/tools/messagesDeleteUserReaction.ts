/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesDeleteUserReaction } from "../../funcs/messagesDeleteUserReaction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteUserMessageReactionRequest$inboundSchema,
};

export const tool$messagesDeleteUserReaction: ToolDefinition<typeof args> = {
  name: "messages-delete-user-reaction",
  description:
    `Deletes another user's reaction. This endpoint requires the MANAGE_MESSAGES permission to be present on the current user.`,
  scopes: ["messages", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesDeleteUserReaction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
