/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsCreateTest } from "../../funcs/entitlementsCreateTest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateEntitlementRequest$inboundSchema,
};

export const tool$entitlementsCreateTest: ToolDefinition<typeof args> = {
  name: "entitlements-create-test",
  description:
    `Creates a test entitlement to a given SKU for a given guild or user. Discord will act as though that user or guild has entitlement to your premium offering. This endpoint returns a partial entitlement object.`,
  scopes: ["entitlements", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await entitlementsCreateTest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
