/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesCreateGuild } from "../../funcs/guildTemplatesCreateGuild.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGuildFromTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesCreateGuild: ToolDefinition<typeof args> = {
  name: "guild-templates-create-guild",
  description:
    `Create a new guild based on a template. Returns a guild object on success. Fires a Guild Create Gateway event.`,
  scopes: ["guild-template", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesCreateGuild(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
