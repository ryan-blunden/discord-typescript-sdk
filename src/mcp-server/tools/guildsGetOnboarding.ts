/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsGetOnboarding } from "../../funcs/guildsGetOnboarding.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildsOnboardingRequest$inboundSchema,
};

export const tool$guildsGetOnboarding: ToolDefinition<typeof args> = {
  name: "guilds-get-onboarding",
  description: `Returns the Onboarding object for the guild.`,
  scopes: ["guilds", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsGetOnboarding(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
