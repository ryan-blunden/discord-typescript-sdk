/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsListJoinedPrivateArchivedThreads } from "../../funcs/channelsListJoinedPrivateArchivedThreads.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListMyPrivateArchivedThreadsRequest$inboundSchema,
};

export const tool$channelsListJoinedPrivateArchivedThreads: ToolDefinition<
  typeof args
> = {
  name: "channels-list-joined-private-archived-threads",
  description:
    `Returns archived threads in the channel that are of type PRIVATE_THREAD, and the user has joined. Threads are ordered by their id, in descending order. Requires the READ_MESSAGE_HISTORY permission.`,
  scopes: ["channels", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsListJoinedPrivateArchivedThreads(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
