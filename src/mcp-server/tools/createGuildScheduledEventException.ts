/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { createGuildScheduledEventException } from "../../funcs/createGuildScheduledEventException.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGuildScheduledEventExceptionRequest$inboundSchema,
};

export const tool$createGuildScheduledEventException: ToolDefinition<
  typeof args
> = {
  name: "create-guild-scheduled-event-exception",
  description: `Create an exception to a recurring guild scheduled event`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await createGuildScheduledEventException(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
