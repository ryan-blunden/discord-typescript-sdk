/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soundboardsListGuildSoundboardSounds } from "../../funcs/soundboardsListGuildSoundboardSounds.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildSoundboardSoundsRequest$inboundSchema,
};

export const tool$soundboardsListGuildSoundboardSounds: ToolDefinition<
  typeof args
> = {
  name: "soundboards-list-guild-soundboard-sounds",
  description:
    `Returns a list of soundboard sound objects for the given guild.`,
  scopes: ["soundboard", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await soundboardsListGuildSoundboardSounds(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
