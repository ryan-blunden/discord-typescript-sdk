/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invitesGet } from "../../funcs/invitesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.InviteResolveRequest$inboundSchema,
};

export const tool$invitesGet: ToolDefinition<typeof args> = {
  name: "invites-get",
  description: `Returns an invite object for the given code.`,
  scopes: ["invites", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await invitesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
