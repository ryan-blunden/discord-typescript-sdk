/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { soundboardsGetSoundboardDefaultSounds } from "../../funcs/soundboardsGetSoundboardDefaultSounds.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$soundboardsGetSoundboardDefaultSounds: ToolDefinition = {
  name: "soundboards-get-soundboard-default-sounds",
  description:
    `Returns a list of the default sounds available in the soundboard.`,
  scopes: ["soundboard", "read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await soundboardsGetSoundboardDefaultSounds(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
