/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { bulkAddInviteTargetUsers } from "../../funcs/bulkAddInviteTargetUsers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.BulkAddInviteTargetUsersRequest$inboundSchema,
};

export const tool$bulkAddInviteTargetUsers: ToolDefinition<typeof args> = {
  name: "bulk-add-invite-target-users",
  description: `Add multiple target users to an existing invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await bulkAddInviteTargetUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
