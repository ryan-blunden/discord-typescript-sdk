/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationUserRoleConnectionUpdate } from "../../funcs/applicationUserRoleConnectionUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateApplicationUserRoleConnectionRequest$inboundSchema,
};

export const tool$applicationUserRoleConnectionUpdate: ToolDefinition<
  typeof args
> = {
  name: "application-user-role-connection-update",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationUserRoleConnectionUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
