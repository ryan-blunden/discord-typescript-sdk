/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsCreateInviteMultipart } from "../../funcs/channelsCreateInviteMultipart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateChannelInviteMultipartRequest$inboundSchema,
};

export const tool$channelsCreateInviteMultipart: ToolDefinition<typeof args> = {
  name: "channels-create-invite-multipart",
  description: `Create a new invite object for the channel.`,
  scopes: ["channels", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsCreateInviteMultipart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value?.result;

    return formatResult(value, apiCall);
  },
};
