/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { stickerPacksGet } from "../../funcs/stickerPacksGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetStickerPackRequest$inboundSchema,
};

export const tool$stickerPacksGet: ToolDefinition<typeof args> = {
  name: "sticker-packs_get",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await stickerPacksGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
