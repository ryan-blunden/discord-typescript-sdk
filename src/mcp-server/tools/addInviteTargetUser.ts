/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addInviteTargetUser } from "../../funcs/addInviteTargetUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddInviteTargetUserRequest$inboundSchema,
};

export const tool$addInviteTargetUser: ToolDefinition<typeof args> = {
  name: "add-invite-target-user",
  description: `Add a target user to an existing invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await addInviteTargetUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
