/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsGetMember } from "../../funcs/guildsGetMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildMemberRequest$inboundSchema,
};

export const tool$guildsGetMember: ToolDefinition<typeof args> = {
  name: "guilds-get-member",
  description: `Returns a guild member object for the specified user.`,
  scopes: ["guilds", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsGetMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
