/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetMessage } from "../../funcs/webhooksGetMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWebhookMessageRequest$inboundSchema,
};

export const tool$webhooksGetMessage: ToolDefinition<typeof args> = {
  name: "webhooks-get-message",
  description:
    `Returns a previously-sent webhook message from the same token. Returns a message object on success.`,
  scopes: ["webhooks", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksGetMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
