/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsAddGroupDMRecipient } from "../../funcs/channelsAddGroupDMRecipient.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddGroupDmUserRequest$inboundSchema,
};

export const tool$channelsAddGroupDMRecipient: ToolDefinition<typeof args> = {
  name: "channels-add-group-DM-recipient",
  description: `Adds a recipient to a Group DM using their access token.`,
  scopes: ["channels", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsAddGroupDMRecipient(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
