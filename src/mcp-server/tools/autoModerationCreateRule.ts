/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { autoModerationCreateRule } from "../../funcs/autoModerationCreateRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateAutoModerationRuleRequest$inboundSchema,
};

export const tool$autoModerationCreateRule: ToolDefinition<typeof args> = {
  name: "auto-moderation-create-rule",
  description:
    `Create a new rule. Returns an auto moderation rule on success. Fires an Auto Moderation Rule Create Gateway event.`,
  scopes: ["auto-moderation", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await autoModerationCreateRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
