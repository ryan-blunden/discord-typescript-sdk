/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { autoModerationDeleteRule } from "../../funcs/autoModerationDeleteRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAutoModerationRuleRequest$inboundSchema,
};

export const tool$autoModerationDeleteRule: ToolDefinition<typeof args> = {
  name: "auto-moderation-delete-rule",
  description:
    `Delete a rule. Returns a 204 on success. Fires an Auto Moderation Rule Delete Gateway event.`,
  scopes: ["auto-moderation", "write", "auto-moderation", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await autoModerationDeleteRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
