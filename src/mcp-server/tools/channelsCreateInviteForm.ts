/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsCreateInviteForm } from "../../funcs/channelsCreateInviteForm.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateChannelInviteFormRequest$inboundSchema,
};

export const tool$channelsCreateInviteForm: ToolDefinition<typeof args> = {
  name: "channels-create-invite-form",
  description: `Create a new invite object for the channel.`,
  scopes: ["channels", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await channelsCreateInviteForm(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value?.result;

    return formatResult(value, apiCall);
  },
};
