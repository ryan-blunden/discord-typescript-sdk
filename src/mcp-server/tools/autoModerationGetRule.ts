/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { autoModerationGetRule } from "../../funcs/autoModerationGetRule.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAutoModerationRuleRequest$inboundSchema,
};

export const tool$autoModerationGetRule: ToolDefinition<typeof args> = {
  name: "auto-moderation-get-rule",
  description:
    `Get a single rule. Returns an auto moderation rule object. This endpoint requires the MANAGE_GUILD permission.`,
  scopes: ["auto-moderation", "read", "auto-moderation", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await autoModerationGetRule(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
