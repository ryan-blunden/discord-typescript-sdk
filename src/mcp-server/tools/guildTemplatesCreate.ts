/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesCreate } from "../../funcs/guildTemplatesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGuildTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesCreate: ToolDefinition<typeof args> = {
  name: "guild-templates-create",
  description:
    `Creates a template for the guild. Requires the MANAGE_GUILD permission. Returns the created guild template object on success.`,
  scopes: ["guild-template", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
