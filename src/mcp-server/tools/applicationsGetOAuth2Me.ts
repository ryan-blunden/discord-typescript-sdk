/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsGetOAuth2Me } from "../../funcs/applicationsGetOAuth2Me.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$applicationsGetOAuth2Me: ToolDefinition = {
  name: "applications-get-O-auth2-me",
  description:
    `Returns the OAuth2 application object associated with the requesting bot user.`,
  scopes: ["application", "read"],
  tool: async (client, ctx) => {
    const [result, apiCall] = await applicationsGetOAuth2Me(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
