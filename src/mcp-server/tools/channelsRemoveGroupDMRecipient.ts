/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsRemoveGroupDMRecipient } from "../../funcs/channelsRemoveGroupDMRecipient.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteGroupDmUserRequest$inboundSchema,
};

export const tool$channelsRemoveGroupDMRecipient: ToolDefinition<typeof args> =
  {
    name: "channels-remove-group-DM-recipient",
    description: `Removes a recipient from a Group DM.`,
    scopes: ["channels", "write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await channelsRemoveGroupDMRecipient(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
