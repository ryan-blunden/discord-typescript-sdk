/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { channelsMessagesUpdateMultipart } from "../../funcs/channelsMessagesUpdateMultipart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateMessageMultipartRequest$inboundSchema,
};

export const tool$channelsMessagesUpdateMultipart: ToolDefinition<typeof args> =
  {
    name: "channels-messages-update-multipart",
    description: ``,
    scopes: ["write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await channelsMessagesUpdateMultipart(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
