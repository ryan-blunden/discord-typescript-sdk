/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceGetCurrentUserState } from "../../funcs/voiceGetCurrentUserState.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSelfVoiceStateRequest$inboundSchema,
};

export const tool$voiceGetCurrentUserState: ToolDefinition<typeof args> = {
  name: "voice-get-current-user-state",
  description: `Returns the current user's voice state in the guild.`,
  scopes: ["voice", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await voiceGetCurrentUserState(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
