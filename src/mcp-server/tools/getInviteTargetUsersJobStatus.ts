/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { getInviteTargetUsersJobStatus } from "../../funcs/getInviteTargetUsersJobStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInviteTargetUsersJobStatusRequest$inboundSchema,
};

export const tool$getInviteTargetUsersJobStatus: ToolDefinition<typeof args> = {
  name: "get-invite-target-users-job-status",
  description: `Get the target users job status for an invite.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await getInviteTargetUsersJobStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
