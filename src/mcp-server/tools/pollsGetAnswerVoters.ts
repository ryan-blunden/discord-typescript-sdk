/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pollsGetAnswerVoters } from "../../funcs/pollsGetAnswerVoters.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAnswerVotersRequest$inboundSchema,
};

export const tool$pollsGetAnswerVoters: ToolDefinition<typeof args> = {
  name: "polls-get-answer-voters",
  description:
    `Get a list of users that voted for a specific answer in a poll.`,
  scopes: ["polls", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await pollsGetAnswerVoters(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
