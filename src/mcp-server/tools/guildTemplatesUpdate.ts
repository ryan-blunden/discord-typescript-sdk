/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesUpdate } from "../../funcs/guildTemplatesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGuildTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesUpdate: ToolDefinition<typeof args> = {
  name: "guild-templates-update",
  description:
    `Modifies the template's metadata. Requires the MANAGE_GUILD permission. Returns the guild template object on success.`,
  scopes: ["guild-template", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
