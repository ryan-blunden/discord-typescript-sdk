/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesCreateMultipart } from "../../funcs/messagesCreateMultipart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateMessageMultipartRequest$inboundSchema,
};

export const tool$messagesCreateMultipart: ToolDefinition<typeof args> = {
  name: "messages-create-multipart",
  description:
    `Post a message to a guild text or DM channel. If operating on a guild channel, this endpoint requires the SEND_MESSAGES permission to be present on the current user.`,
  scopes: ["messages", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesCreateMultipart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
