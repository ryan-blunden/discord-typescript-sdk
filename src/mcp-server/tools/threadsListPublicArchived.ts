/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { threadsListPublicArchived } from "../../funcs/threadsListPublicArchived.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListPublicArchivedThreadsRequest$inboundSchema,
};

export const tool$threadsListPublicArchived: ToolDefinition<typeof args> = {
  name: "threads-list-public-archived",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await threadsListPublicArchived(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
