/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesDelete } from "../../funcs/guildTemplatesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteGuildTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesDelete: ToolDefinition<typeof args> = {
  name: "guild-templates-delete",
  description:
    `Deletes the template. Requires the MANAGE_GUILD permission. Returns the deleted guild template object on success.`,
  scopes: ["guild-template", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
