/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksUpdateOriginalMessageForm } from "../../funcs/webhooksUpdateOriginalMessageForm.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateOriginalWebhookMessageFormRequest$inboundSchema,
};

export const tool$webhooksUpdateOriginalMessageForm: ToolDefinition<
  typeof args
> = {
  name: "webhooks-update-original-message-form",
  description: `Edits the initial webhook message.`,
  scopes: ["webhooks", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksUpdateOriginalMessageForm(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
