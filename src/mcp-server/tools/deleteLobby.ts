/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deleteLobby } from "../../funcs/deleteLobby.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLobbyRequest$inboundSchema,
};

export const tool$deleteLobby: ToolDefinition<typeof args> = {
  name: "delete-lobby",
  description:
    `Deletes the specified lobby if it exists. It is safe to call even if the lobby is already deleted.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deleteLobby(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
