/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsSoundboardSoundsUpdate } from "../../funcs/guildsSoundboardSoundsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGuildSoundboardSoundRequest$inboundSchema,
};

export const tool$guildsSoundboardSoundsUpdate: ToolDefinition<typeof args> = {
  name: "guilds-soundboard-sounds-update",
  description: ``,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsSoundboardSoundsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
