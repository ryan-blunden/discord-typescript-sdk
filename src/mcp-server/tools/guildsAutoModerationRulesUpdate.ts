/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsAutoModerationRulesUpdate } from "../../funcs/guildsAutoModerationRulesUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAutoModerationRuleRequest$inboundSchema,
};

export const tool$guildsAutoModerationRulesUpdate: ToolDefinition<typeof args> =
  {
    name: "guilds-auto-moderation-rules-update",
    description: ``,
    scopes: ["write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await guildsAutoModerationRulesUpdate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
