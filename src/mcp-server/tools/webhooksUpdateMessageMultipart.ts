/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksUpdateMessageMultipart } from "../../funcs/webhooksUpdateMessageMultipart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateWebhookMessageMultipartRequest$inboundSchema,
};

export const tool$webhooksUpdateMessageMultipart: ToolDefinition<typeof args> =
  {
    name: "webhooks-update-message-multipart",
    description:
      `Edits a previously-sent webhook message from the same token. Returns a message object on success.`,
    scopes: ["webhooks", "write"],
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await webhooksUpdateMessageMultipart(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
