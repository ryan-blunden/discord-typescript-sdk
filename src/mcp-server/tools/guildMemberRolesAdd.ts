/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildMemberRolesAdd } from "../../funcs/guildMemberRolesAdd.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddGuildMemberRoleRequest$inboundSchema,
};

export const tool$guildMemberRolesAdd: ToolDefinition<typeof args> = {
  name: "guild-member-roles-add",
  description: ``,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildMemberRolesAdd(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
