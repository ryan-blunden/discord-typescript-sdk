/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { lobbiesCreate } from "../../funcs/lobbiesCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLobbyRequestBody$inboundSchema,
};

export const tool$lobbiesCreate: ToolDefinition<typeof args> = {
  name: "lobbies-create",
  description: `Creates a new lobby. Returns a lobby object on success.`,
  scopes: ["lobby", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await lobbiesCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
