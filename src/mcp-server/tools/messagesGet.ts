/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { messagesGet } from "../../funcs/messagesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetMessageRequest$inboundSchema,
};

export const tool$messagesGet: ToolDefinition<typeof args> = {
  name: "messages-get",
  description:
    `Returns a specific message in the channel. If operating on a guild channel, this endpoint requires the READ_MESSAGE_HISTORY permission to be present on the current user.`,
  scopes: ["messages", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await messagesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
