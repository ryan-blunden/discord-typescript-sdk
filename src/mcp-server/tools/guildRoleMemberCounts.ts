/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildRoleMemberCounts } from "../../funcs/guildRoleMemberCounts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GuildRoleMemberCountsRequest$inboundSchema,
};

export const tool$guildRoleMemberCounts: ToolDefinition<typeof args> = {
  name: "guild-role-member-counts",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildRoleMemberCounts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
