/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildScheduledEventsList } from "../../funcs/guildScheduledEventsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildScheduledEventsRequest$inboundSchema,
};

export const tool$guildScheduledEventsList: ToolDefinition<typeof args> = {
  name: "guild-scheduled-events-list",
  description:
    `Returns a list of guild scheduled event objects for the given guild.`,
  scopes: ["guild-scheduled-event", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildScheduledEventsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
