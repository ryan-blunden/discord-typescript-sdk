/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { emojiCreateGuildEmoji } from "../../funcs/emojiCreateGuildEmoji.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateGuildEmojiRequest$inboundSchema,
};

export const tool$emojiCreateGuildEmoji: ToolDefinition<typeof args> = {
  name: "emoji-create-guild-emoji",
  description:
    `Create a new emoji for the guild. Requires the CREATE_GUILD_EXPRESSIONS permission. Returns the new emoji object on success. Fires a Guild Emojis Update Gateway event.`,
  scopes: ["emoji", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await emojiCreateGuildEmoji(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
