/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsUpdateIncidentActions } from "../../funcs/guildsUpdateIncidentActions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateGuildIncidentActionsRequest$inboundSchema,
};

export const tool$guildsUpdateIncidentActions: ToolDefinition<typeof args> = {
  name: "guilds-update-incident-actions",
  description:
    `Modifies the incident actions of the guild. Returns a 200 with the Incidents Data object for the guild. Requires the MANAGE_GUILD permission.`,
  scopes: ["guilds", "write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildsUpdateIncidentActions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
