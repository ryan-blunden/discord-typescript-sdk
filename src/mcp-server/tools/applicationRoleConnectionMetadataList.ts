/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationRoleConnectionMetadataList } from "../../funcs/applicationRoleConnectionMetadataList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetApplicationRoleConnectionsMetadataRequest$inboundSchema,
};

export const tool$applicationRoleConnectionMetadataList: ToolDefinition<
  typeof args
> = {
  name: "application-role-connection-metadata-list",
  description:
    `Returns a list of application role connection metadata objects for the given application.`,
  scopes: ["application-role-connection-metadata", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationRoleConnectionMetadataList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
