/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildTemplatesGet } from "../../funcs/guildTemplatesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetGuildTemplateRequest$inboundSchema,
};

export const tool$guildTemplatesGet: ToolDefinition<typeof args> = {
  name: "guild-templates-get",
  description: `Returns a guild template object for the given code.`,
  scopes: ["guild-template", "read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await guildTemplatesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
