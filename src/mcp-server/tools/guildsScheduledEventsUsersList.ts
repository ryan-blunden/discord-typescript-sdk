/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { guildsScheduledEventsUsersList } from "../../funcs/guildsScheduledEventsUsersList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListGuildScheduledEventUsersRequest$inboundSchema,
};

export const tool$guildsScheduledEventsUsersList: ToolDefinition<typeof args> =
  {
    name: "guilds-scheduled-events-users-list",
    description: ``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await guildsScheduledEventsUsersList(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
