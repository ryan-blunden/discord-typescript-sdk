/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { updateUserMessageExternalModerationMetadata } from "../../funcs/updateUserMessageExternalModerationMetadata.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.UpdateUserMessageExternalModerationMetadataRequest$inboundSchema,
};

export const tool$updateUserMessageExternalModerationMetadata: ToolDefinition<
  typeof args
> = {
  name: "update-user-message-external-moderation-metadata",
  description:
    `Update the external moderation metadata for a user message (DM).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await updateUserMessageExternalModerationMetadata(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
