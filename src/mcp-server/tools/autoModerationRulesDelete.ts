/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { autoModerationRulesDelete } from "../../funcs/autoModerationRulesDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAutoModerationRuleRequest$inboundSchema,
};

export const tool$autoModerationRulesDelete: ToolDefinition<typeof args> = {
  name: "auto-moderation-rules_delete",
  description: ``,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await autoModerationRulesDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
